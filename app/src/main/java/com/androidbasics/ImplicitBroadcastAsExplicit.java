/*
package com.androidbasics;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.widget.Toast;

//      Calling Implicit Broadcast as Explicit Broadcast(Receiver Code Written in Branch CustomBroadcastSender Manifest File  & This  App Manifest File  )
public class ImplicitBroadcastAsExplicit extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        Toast.makeText(context, "ImplicitBroadcastAsExplicit Called", Toast.LENGTH_SHORT).show();
    }
}
*/
